const path = require('path');

// webpack@"^3.10.0" is needed
module.exports = {
  entry: {
    myTools: path.resolve(__dirname, './index.js')
  },
  output: {
    path: path.resolve(__dirname, 'dist'),
    publicPath: "/",
    filename: "axios.js",
    chunkFilename: "axios.min.js",
    libraryTarget: "umd",
    library: "axios"
  }
};